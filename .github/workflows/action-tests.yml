name: Python Tests
on: [push]

jobs:
  name: "Run tests"
  runs-on: ubuntu-latest
  
  steps:
  - uses: actions/setup-python@v2
    with:
      python-version: '3.9.6'

    run: |
     pip install -r requirements.txt
  
    run: |
      pytest -r w
  
